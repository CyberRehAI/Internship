You already have complete knowledge of this project and the existing experimental hierarchical cascade.

Current implemented cascade:

Network Features (7)
↓
LSTM Autoencoder
↓ latent z1
Protocol Features (122) + z1
↓
USAD
↓ latent z2
Physical Features (65) + z2
↓
TranAD
↓ latent z3
Mahalanobis Distance
↓
Threshold
↓
Decision

The cascade is already fully implemented and working.

DO NOT modify:

- model architectures
- latent propagation
- preprocessing
- feature selection
- scaling
- window generation
- hyperparameters
- training pipeline

The original experiment must remain completely untouched.

======================================================
NEW REQUIREMENT
======================================================

Implement a NEW experiment mode called:

Hourly Behavior Generalization

This experiment must exist alongside the current implementation and must not replace it.

======================================================
OBJECTIVE
======================================================

The goal is NOT to evaluate anomaly detection.

The goal is to evaluate whether the hierarchical cascade learns a behavioral representation that generalizes to the immediately following hour of the SAME operating condition.

The SWaT dataset consists of eight one-hour periods:

Hour 1
09:00–10:00
Normal

Hour 2
10:00–11:00
Normal

Hour 3
11:00–12:00
Normal

Hour 4
12:00–13:00
Normal

Hour 5
13:00–14:00
Attack

Hour 6
14:00–15:00
Attack

Hour 7
15:00–16:00
Attack

Hour 8
16:00–17:00
Attack

======================================================
EXPERIMENTS
======================================================

Perform FOUR completely independent experiments.

Experiment 1

Train the complete cascade ONLY on Hour 1.

Evaluate ONLY on Hour 2.

------------------------------------------------------

Experiment 2

Train the complete cascade ONLY on Hour 3.

Evaluate ONLY on Hour 4.

------------------------------------------------------

Experiment 3

Train the complete cascade ONLY on Hour 5.

Evaluate ONLY on Hour 6.

------------------------------------------------------

Experiment 4

Train the complete cascade ONLY on Hour 7.

Evaluate ONLY on Hour 8.

======================================================
IMPORTANT
======================================================

Every experiment starts from completely fresh random weights.

Do NOT reuse checkpoints.

Do NOT fine-tune from previous experiments.

Each experiment is completely independent.

For every experiment:

Train LSTM-AE

↓

Freeze encoder

↓

Train USAD

↓

Freeze encoder

↓

Train TranAD

↓

Freeze encoder

↓

Fit Mahalanobis on the training hour latent representations

↓

Compute anomaly scores on the evaluation hour

Use exactly the same hyperparameters currently implemented.

======================================================
THRESHOLD
======================================================

There is NO validation hour.

For each experiment:

- Fit Mahalanobis using ONLY the training hour latent vectors.
- Compute the anomaly threshold as the 95th percentile of the Mahalanobis distances from the TRAINING hour.
- Apply that threshold to the evaluation hour.

Do NOT split the training hour into train/validation.

Do NOT introduce any additional validation logic.

======================================================
WINDOWS
======================================================

Reuse the existing Phase 3 windows exactly as implemented.

Keep the same full-containment policy.

No window may cross hourly boundaries.

======================================================
REPORT FOR EACH EXPERIMENT
======================================================

Generate:

- Mean reconstruction loss per stage
- Mean Mahalanobis distance
- Median Mahalanobis distance
- Standard deviation of Mahalanobis distance
- Percentage of windows exceeding the threshold
- Number of anomalous windows
- Mahalanobis score histogram
- Reconstruction loss histogram

======================================================
SAVE OUTPUTS
======================================================

Create:

baselines/outputs/hourly_generalization/

experiment_1/

experiment_2/

experiment_3/

experiment_4/

Each experiment should save:

- checkpoints
- metrics.json
- summary.csv
- Mahalanobis histogram
- reconstruction histogram
- embeddings.npz

======================================================
FINAL REPORT
======================================================

Generate:

hourly_generalization_report.md

Include a comparison table:

| Experiment | Train Hour | Test Hour | Operating Regime | Mean Mahalanobis | Std | % Above Threshold | Mean Reconstruction Loss |

Then automatically write a short interpretation for every experiment explaining whether the learned latent representation generalized successfully to the following hour of the same operating regime or whether significant behavioral drift occurred.

======================================================
IMPORTANT

This is an ADDITIONAL experiment only.

Do not modify the existing training pipeline or the original anomaly detection experiment.

The original experiment and all existing outputs must continue to work exactly as before.